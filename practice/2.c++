#include <iostream>
using namespace std;
int main()
{
    float a, b, c;
    cout << "enter number a:";
    cin >> a;
    cout << "enter number b:";
    cin >> b;
    cout << "enter number c:";
    cin >> c;
    float avg;
    avg = (a + b + c) / 3;
    cout << " the average of three number is :" << avg;
    return 0;
}
